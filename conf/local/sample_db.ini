;* ------------------------------------------------------------------------- *
;*	DB設定
;* ------------------------------------------------------------------------- */

;// ホスト名
DB_SERVER = "localhost"

;// データベース名
DB_NAME = "db"

;// ユーザー名
DB_USER = "root"

;// パスワード
DB_PASS = "pass"

;// テーブルの接頭辞
DB_PREFIX = "hanauta_"

;// DBキャラクターセット
DB_CHARSET = "utf8"
